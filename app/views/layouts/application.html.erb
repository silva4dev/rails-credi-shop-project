<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <title>Credi-Shop</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= vite_client_tag %>
    <%= vite_typescript_tag "application" %>
    <%= vite_stylesheet_tag "application.scss" %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" %>
    <%= stylesheet_link_tag "https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" %>
  </head>
  <body>
    <div class="d-flex flex-column" style="height: 100vh;">
      <% if flash[:success] %>
        <div class="p-4 bg-success font-medium rounded-lg text-center text-white" id="notice"><%= flash[:success] %></div>
      <% end %>
      <% if flash[:error] %>
        <div class="p-4 bg-danger font-medium rounded-lg text-center text-white" id="alert"><%= flash[:error] %></div>
      <% end %>
      <nav class="navbar navbar-dark bg-dark shadow-sm p-4">
        <div class="container d-flex justify-content-between align-items-center"> <!-- Adicionamos container para centralizar -->
          <%= link_to Sessions::Infrastructure::Models::Current.user ? proponents_path : root_path do %>
            <%= vite_image_tag "images/logo.png", style: "width: 150px; height: auto; margin-bottom: 15px;" %>
          <% end %>
          <% if Sessions::Infrastructure::Models::Current.user %>
            <div>
              <%= button_to "Logout", logout_path, method: :delete, class: "btn btn-primary" %>
            </div>
          <% end %>
        </div>
      </nav>
      <main class="flex-grow-1 p-5">
        <div class="container mt-3">
          <%= yield %>
        </div>
      </main>
      <footer class="p-4 bg-dark d-flex justify-content-end align-items-center text-white">
        <p>
          Desenvolvido com <i class="fa fa-heart text-danger" style="margin: 0 2px;"></i>
          por <span style="font-weight: bold">Lucas Alves</span>
        </p>
      </footer>
    </div>
    <%= javascript_include_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js' %>
    <%= javascript_include_tag 'https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js' %>
    <%= javascript_include_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js' %>
  </body>
</html>
